<h1>Songs</h1>
<ul>
<% @songs.each do |song| %>
  <li>
    <%= song.album.band.name %>/<%= song.album.name %><br><%= link_to song.name, song_path(song) %><br><br><%= audio_tag(song.track, autoplay: false, controls: true) %>
  </li><br>
<% end %>
</ul>